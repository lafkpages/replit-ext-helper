{
  "name": "replit-ext-helper",
  "version": "0.0.1",
  "author": "LuisAFK",
  "main": "index.js",
  "files": [
    "src"
  ],
  "devDependencies": {
    "esbuild": "^0.19.3"
  },
  "scripts": {
    "build": "esbuild src/index.js --minify --outdir=dist",
    "prepublishOnly": "bun run build"
  }
}
